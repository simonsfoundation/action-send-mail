{
  "name": "action-send-mail",
  "version": "1.0.0",
  "description": "GitHub Action to send email via SMTP",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "lint": "eslint src/",
    "test": "jest"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "nodemailer": "^6.9.14",
    "glob": "^10.4.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.2",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "license": "MIT"
}
